{
  "version": "0.0.1",
  "//": "Two entries, not one `npm run dev`: the launcher exports PORT for the config it starts, and a single entry would hand the same port to both Vite and the API (which then fight over 5173 and the /api proxy gets nothing). Start `api` first, then `web`.",
  "configurations": [
    {
      "name": "api",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "dev", "--workspace", "server"],
      "port": 3000,
      "autoPort": false
    },
    {
      "name": "web",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "dev", "--workspace", "client"],
      "port": 5173,
      "//": "Port is fixed: APP_URL and the Google OAuth redirect URI both point at localhost:5173.",
      "autoPort": false
    }
  ]
}
